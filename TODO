- test if the current entry already exists (title+year?)

- test that current given id does not exist.

- Return a true value when database entry is correctly saved. 

- Get the database name as script argument.

- Return a true value when Reference instance is correctly created.

- make SQLiteManager check if the given Reference instance has all the required parameters.

- [ALTERNATIVE] substitute SQLiteManager for a SQLObject-derived class or something similar.

- make form in HTML
